package ch.clicktotranslate.translation.framework.spring.http.outbound.dto;

/**
 * HTTP-specific DTO for DeepL translate API response.
 * Maps to the actual HTTP response body format returned by DeepL's REST API.
 */
public class DeepLHttpTranslateResponse {
  // TODO: Add fields matching DeepL's HTTP API response
  // e.g., translations array, detected_source_language, etc.
}
