package ch.clicktotranslate.translation.framework.spring.http.outbound.dto;

/**
 * HTTP-specific DTO for DeepL translate API request.
 * Maps to the actual HTTP request body format expected by DeepL's REST API.
 */
public class DeepLHttpTranslateRequest {
  // TODO: Add fields matching DeepL's HTTP API contract
  // e.g., text, source_lang, target_lang, formality, etc.
}
